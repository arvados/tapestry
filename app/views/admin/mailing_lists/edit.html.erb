<h1>Edit mailing list</h1>

<%= form_for([:admin, @mailing_list]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', admin_mailing_lists_path %>
