<%= error_messages_for :tissue_type, :header_message => 'Error: data could not be saved.', :message => nil %>

<%= form_for(@tissue_type) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
