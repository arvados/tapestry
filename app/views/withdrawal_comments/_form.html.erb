<%= form_for(@withdrawal_comment) do |f| %>
  <div class="field">
    <%= f.label 'Your comments:' %><br />
    <%= f.text_area :comment, :cols => 72, :rows => 12 %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit comments' %>
  </div>
<% end %>
