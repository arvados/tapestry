<h2>Double-check your email</h2>

<p>Please check over your name and email; we will send a confirmation email to <strong><%=h @user.full_name %></strong>
at <%= link_to h(@user.email), "mailto:#{h(@user.email)}" %> that you will need to check in order to complete your registration.</p>

<h2>Choose a password</h2>
<p>Please enter a password for your PGP account:</p>

<% form_for :user, :url => users_url, :html => { :class => 'signup' } do |f| -%>

<% %w(first_name middle_name last_name email).each do |attr| -%>
  <%= f.hidden_field attr %>
<% end -%>

<p><%= f.label :password %>
<% if @user.errors.on(:password).nil? -%>(minimum is 6 characters)<% end -%>
<%= error_message_on :user, :password, :append_text => '.' %>
<br/>
<%= f.password_field :password %></p>

<p><%= f.label :password_confirmation, 'Confirm password' %>
<%= error_message_on :user, :password_confirmation, :append_text => '.' %>
<br/>
<%= f.password_field :password_confirmation %></p>

<h2>Are you a robot?</h2>
<p>Please type the two words you see below to confirm that you&rsquo;re a real person.</p>
<%= recaptcha_tags %>

<p><%= submit_tag 'Sign up' %></p>
<% end -%>
